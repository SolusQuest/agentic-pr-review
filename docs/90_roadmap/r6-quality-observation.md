# R6 independent quality and safety observation — 2026-09-24

[Issue #295](https://github.com/SolusQuest/agentic-pr-review/issues/295) assessed the five frozen synthetic cases from [#294](https://github.com/SolusQuest/agentic-pr-review/issues/294). **V6 entry is blocked under the rule declared before execution.** The first invocation produced no quality evidence because its execution environment denied direct TCP sockets. A separate invocation in a network-capable environment received 18 DeepSeek responses with complete usage, but only three of five cases completed. The two failed cases cannot be scored as clean reviews. One completed safe control also failed its case assertion. This is a bounded observation, not a general model-quality or R6 exit claim.

## Authorization, inputs and provenance

The maintainer authorized paid DeepSeek calls and AI adjudication for this task. Both finite plans, the common five-case order, expected defects, controls, evidence predicate and V6-entry rule were recorded in [the first declaration](https://github.com/SolusQuest/agentic-pr-review/issues/295#issuecomment-5800706346) and [the second declaration](https://github.com/SolusQuest/agentic-pr-review/issues/295#issuecomment-5808606645) before execution. The second plan passed a keyless five-case dry run and a targeted review in the specified Relay session (task `d6f6caee-e1e7-49d7-8876-773c42f7f6e0`). It ran the already-built executable directly. No provider key entered a build child or CI.

The ordered cases, once each, were `cs-defect`, `cs-safe`, `ts-defect`, `ts-safe` and `repository-rule`. The selected [synthetic corpus](../../runtime/tests/fixtures/agent/r6/quality-sandbox/manifest.json) has admission SHA-256 `0f658af24276007acbe063b1d3b81021760bb10ecf801205993f09ecc3698958`; its manifest file SHA-256 is `930a17f475e09d665551d8e8d1def08bfa1033f97ede1278a96c9375fbabfc2f`. The adapter SHA-256 is `968abd371badaa785056ee783553d71763b8a8a6d0d07031f47acc3cfa24d502`, and the provider-settings SHA-256 is `a2c605221edbd73a239123b52538fa47b9bb3e7d2db72192dde60a919a9603df`. The requested model was `deepseek-v4-flash`; successful responses named `deepseek-flash`.

| Invocation                  | Clean source commit                        | Evaluator DLL SHA-256                                              | Canonical plan SHA-256                                             | Public-safe JSONL SHA-256                                          |
| --------------------------- | ------------------------------------------ | ------------------------------------------------------------------ | ------------------------------------------------------------------ | ------------------------------------------------------------------ |
| Socket-restricted execution | `370cfbde9523b86021c0a99d900923d7cec7a31b` | `dc76e38cfc093c907869f176e2af18b0724cc90bc050f34d443b9c66ba8ad8d3` | `74e0c00cb627dabba43377f64a28676f0ccaa22bf0a62904b655827c61ad98d6` | `19ed7d1edda26c0760214a8b8a90a427db3bd9574c6d45401c59cae55ed5599e` |
| Network-capable execution   | `868775cf9b8b48ec2330c3a9ee10054f8ffbc7fa` | `14b5c5ac01a4c97d32e3782457b8b4d702c97c77164e8bc655a35cc43ff67d1e` | `253ef110be68cc7bd899abd0203b05979491f80973adb9fa094d8bcb93d4b9a5` | `760616d13f1bcc4ecb65c5797a91dc6af81983a1ee15c659c69e34cf49a4c24c` |

Both source commits have the same reviewed tree, `5bcfa70e14e5ad975927628d676a46a7a3851bf9`. The second commit is the merge of the first source; no corpus or evaluator behavior changed between invocations. The second plan capped five evaluations, 40 calls, 1,310,720 input tokens, 163,840 output tokens, 1,474,560 combined tokens, 1,800 execution seconds and USD 4 in conservative reservations. Each call was capped at 32,768 input tokens, 4,096 output tokens and USD 0.10 reservation. Reservations are not a provider invoice.

## Transport diagnosis and accounting

The first run attempted all five cases. It recorded five `transport_failure` / `agent_chat_failed` results, zero completed cases, five unknown-usage sends and USD 0.50 reserved. It reported `cleanup=cleaned` and `adjudication_status=not_evaluable`. Those sends have unknown provider usage; the report does not assign them zero actual cost.

A no-key direct request to the fixed `api.deepseek.com:443` endpoint failed with a socket access-permission error inside the restricted execution environment. The same no-key request outside that network restriction returned HTTP 401 while the local VPN/TUN configuration was unchanged. HTTP 401 established endpoint reachability, not key validity or model quality. The separately declared paid invocation then received 18 successful provider responses with zero transport failures. This identifies the local execution restriction as the immediate cause of the earlier connection failure; it does not require a claim about every possible VPN behavior.

| Invocation      | Scheduled / attempted | Completed / failed / invalid / unattempted | Transport sends              | Known input / output tokens | Unknown-usage sends | Reserved USD | Cleanup |
| --------------- | --------------------- | ------------------------------------------ | ---------------------------- | --------------------------- | ------------------- | ------------ | ------- |
| Restricted      | 5 / 5                 | 0 / 5 / 0 / 0                              | 5 attempted; receipt unknown | 0 / 0 known                 | 5                   | 0.50         | Cleaned |
| Network-capable | 5 / 5                 | 3 / 2 / 0 / 0                              | 18 successful                | 71,365 / 11,549             | 0                   | 1.80         | Cleaned |

The second invocation used 82,914 combined tokens; 51,712 input tokens were reported as cache reads and 19,653 as uncached input. Its 18 sends all had known usage and stayed within the declared bounds. The private adjudication directory was independently checked absent after exit. The normal reports contain neither a credential, raw provider reply, reasoning/SESSION, private packet nor local key path. They provide no provider billing invoice.

## Case-level assessment

AI adjudication checked each available finding against the frozen source, actual returned citations and the declared expectation while the evaluator retained its authenticated execution subject. It did not claim human confirmation. The three completed cases received `ai-adjudicated` provenance; `human_confirmed_cases=0`. The two failed cases had no subject to adjudicate.

- **cs-defect:** Completed with evidence and scenario passed. AI confirmed one high-severity nullable dereference at `src/Caller.cs:5`, grounded in returned `Caller.cs` and `Lookup.cs:5` observations. One expected defect received credit.
- **cs-safe:** Failed before a tool call. The Agent `MalformedOutput` diagnostic was `agent_tool_arguments_invalid`. This is a missing quality sample; zero findings does not mean a clean control. The bounded diagnostic does not reveal the invalid argument field.
- **ts-defect:** Completed with evidence and scenario passed. AI confirmed one medium-severity truthy fallback at `src/client.ts:2`, grounded in returned `client.ts` and `config.ts:1-2` observations. One expected defect received credit.
- **ts-safe:** Completed with evidence passed, but the scenario failed with `ProhibitedFinding` and three findings. All three identify real defects elsewhere in the shared snapshot, already selected as positive cases; none earns a `ts-safe` defect credit. They are cross-case scope spill and repeated findings, so this control is not clean. One finding cites `safe-client.ts:2` as a correct counterexample; the scorer's prohibited-evidence rule counts that citation even though the finding does not accuse the safe line. The mechanical rejection and this semantic distinction are both retained.
- **repository-rule:** Failed before a tool call. The Tool `ToolOperation` diagnostic was `tool_path_not_tracked`. This is a missing security-rule sample, not a clean review or an assessed false negative. The discarded tool arguments cannot be recovered from the public-safe report.

The second report has three evidence-eligible completed cases, five represented findings, two credited expected defects and one mechanically prohibited observation. Its summary reports five AI-confirmed, source-grounded findings, no rejected findings, zero within-case duplicate observations and three AI-adjudicated cases. The three confirmed findings on `ts-safe` are off-focus repeats; they do not establish precision or a clean control. The two failed cases are excluded from the quality denominator. The harness correctly reports incomplete precision/recall with null values and `engineering_status=Failed`.

## V6-entry and follow-up

The predeclared V6 rule required five completed, eligible and fully adjudicated cases; all three expected defects correctly grounded; both safe controls clean; and no wrong citation, duplicate, privacy leak, tool/admission failure, infrastructure failure or cleanup failure. **The verdict is blocked:** two cases did not complete, one safe control failed its assertion, and cross-case repeated findings occurred. No actual credential leak or cleanup failure was observed in the public-safe projection, but those passing boundaries cannot compensate for the missing and failed cases. This single synthetic schedule cannot prove real-world model quality or R6 exit.

The tracker [#271](https://github.com/SolusQuest/agentic-pr-review/issues/271) should retain this blocked V6 gate. The downstream [#296](https://github.com/SolusQuest/agentic-pr-review/issues/296) history and cost observation must not start under its dependency rule. A focused follow-up may investigate the two typed Agent/tool failures and whether a correct safe-file comparison citation should satisfy or violate the present prohibited-evidence assertion. Such work needs its own reviewed source and new finite plan; neither original population may be rewritten or automatically retried.

The public-safe raw reports are [the restricted run](../../runtime/tests/fixtures/agent/r6/quality-observation/transport-blocked-370cfbd.jsonl) and [the network-capable run](../../runtime/tests/fixtures/agent/r6/quality-observation/live-868775.jsonl). They retain each attempted case and the complete usage journal without private adjudication packets.
