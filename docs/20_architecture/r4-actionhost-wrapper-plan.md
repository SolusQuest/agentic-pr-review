# R4 ActionHost and thin-wrapper plan

Status: activated normative R4 contract from refinement issue [#138](https://github.com/SolusQuest/agentic-pr-review/issues/138). The implementation graph has reached the W13 cutover audit recorded in [`r4-migration-cutover-handoff.md`](./r4-migration-cutover-handoff.md). The checked-in nested Action and wrapper remain repository-controlled prepared-payload proof only, not a supported downstream Action or R7 public default.

Baseline: `main` at `736508728acc6b2064b75f2e1da81b9342aac70b`, the final R3 implementation and protected no-publish proof commit. R3 supplies the six-tool Agent, exact DeepSeek thinking continuation, authenticated encrypted STATE containing SESSION, a local state-store conformance implementation, and framework-dependent plus Linux x64 Native AOT fresh-process evidence. R4 owns the production GitHub Host, immutable snapshot, downstream-owned artifact state, deterministic publisher, thin wrapper, and replacement of retained TypeScript business paths.

## Decision summary

R4 keeps one Native AOT C# business application. A bundled Node 24 wrapper reads the small Action surface, masks secrets, launches an explicitly prepared and build-identity-checked payload, forwards cancellation, performs a closed set of official Actions artifact operations requested by the Host, renders bounded workflow presentation, and returns the Host exit status. It does not interpret a pull request, choose state, construct a review, validate findings, or decide publication.

The supported credential-bearing entrypoints are a trusted default-branch `workflow_run` workflow and a trusted default-branch `workflow_dispatch` workflow. Direct credential-bearing execution from `pull_request` is not supported because the proposed workflow can modify its own workflow code. R4 reviews same-repository open, non-draft pull requests only; fork-origin work is an observable no-provider/no-state/no-write skip. The Host never executes or imports reviewed repository code.

Both supported entrypoints use the same repository-and-pull-request workflow-level concurrency group with `cancel-in-progress: false`. GitHub's one-running-run guarantee for that shared group is an authoritative R4 precondition for candidate admission and every state or publication side effect, not merely an optimization. The Host admits only a trusted workflow source whose reviewed definition contains the exact supported concurrency contract; a missing, differently scoped, job-only, or unverified group fails before state-key resolution, provider construction, or mutation. Host exact-head, transaction, and readback checks remain mandatory for crash recovery, stale work, and external artifact ambiguity, but they do not claim to create a cross-run lock from artifacts.

Each downstream repository owns its encrypted continuation state in its own GitHub Actions artifacts. GitHub Actions artifacts are R4's sole built-in production/default state-store adapter. In a public repository, artifact existence, opaque names, sizes, digests, timestamps, run provenance, and ciphertext may be public. The protected assets are plaintext, state keys, scope-sensitive logical identifiers, accepted lineage, mutation authority, provider admission, and publication authority. A future Supabase adapter is plausible, but R4 publishes neither that adapter nor a dynamic .NET plugin or external wire compatibility contract.

Sticky publication is enabled by default. Inline comments are an explicit trusted-config opt-in and are limited to grounded high or critical findings. State acceptance follows verified sticky publication; optional inline publication follows acceptance and cannot roll accepted state back. Every cross-service ambiguity is reconciled by exact identities and readback, never by assuming success.

The alternatives rejected for R4 are a permanent TypeScript business Host, direct secret-bearing `pull_request` execution, `pull_request_target`, a reviewed-head checkout as Host authority, GitHub cache or Git refs as continuation state, plaintext artifacts, a hosted central state service, an R4 Supabase dependency, arbitrary dynamically loaded .NET adapters, automatic release download, and implicit `latest` payload selection.

## R4-D01: pre-R7 Action claim and location

R4 restores the Action at `.github/actions/agentic-pr-review/action.yml` with a generated Node 24 bundle at `.github/actions/agentic-pr-review/dist/index.js`. Reusing the historical nested path gives the one identified downstream consumer a deliberate migration path, but it does not preserve the historical input/output contract. The downstream consumer currently pins the immutable historical commit `beb88d5e09e4220ab1f82ad0eafefbf2b10cab8a`; R4 does not alter that pin.

Before R7, the new Action is supported only by repository-controlled proof workflows against an explicitly prepared trusted `linux-x64` payload. The Action metadata must not advertise a runnable downstream example or an automatic download input. `README.md` continues to state that the development head is unsupported as an Action. R7 owns tagged release assets, checksums, exact Action-to-payload mapping, automatic download, compatibility notes, and default promotion. R7 must either retain the nested path or publish an explicit path migration; R4 does not create a root `action.yml` alias.

The wrapper-restoration implementation changes `npm run dist:check` atomically from the current absence guard to generated-bundle reproducibility, checked metadata, and retired-surface drift checks. This design PR leaves the absence guard unchanged.

## R4-D02: invocation and trust-domain matrix

| Entry                                                                      | R4 behavior                                                                                                                                                                                                                                | Secret and write authority                                                                                                       |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| `workflow_run.completed` from the configured unprivileged trigger workflow | Supported when the privileged workflow exists on the default branch, the triggering workflow identity and conclusion are accepted, exactly one associated same-repository PR is open and non-draft, and the trusted source SHA is verified | Provider key, state key, `actions: write`, `contents: read`, and `pull-requests: write` may be supplied only to this trusted job |
| `workflow_dispatch` on the default branch with `pr-number`                 | Supported when the actor has write-or-admin repository permission and the selected PR is same-repository, open, non-draft, and has an available exact head                                                                                 | Same authority as the trusted `workflow_run` route                                                                               |
| `pull_request` or `pull_request_review`                                    | Unsupported for the credential-bearing product route; return `skipped_untrusted_event` before Host key resolution, state mutation, provider construction, or publication                                                                   | Callers must not pass product secrets                                                                                            |
| fork-origin PR from any entry                                              | Return `skipped_fork` in R4                                                                                                                                                                                                                | No state key, provider key, trusted state mutation, or publication authority                                                     |
| closed, merged, draft, missing, ambiguous, or unavailable-head PR          | Return the specific bounded skip or failure before provider work                                                                                                                                                                           | No state mutation or publication                                                                                                 |
| `push`, `schedule`, `issue_comment`, or other events                       | Unsupported                                                                                                                                                                                                                                | No product authority                                                                                                             |

GitHub documents that a `workflow_run` workflow exists on the default branch and can receive secrets and write tokens even when its predecessor cannot. It also warns that executing untrusted code in this privileged workflow is dangerous. R4 therefore treats every triggering payload field and reviewed byte as untrusted data, restores no cache or artifact produced by the trigger workflow, and obtains the reviewed snapshot through Host-owned GitHub reads. See [GitHub's `workflow_run` event documentation](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#workflow_run) and [secure-use guidance](https://docs.github.com/en/actions/reference/security/secure-use).

The trusted workflow pins the Action source and prepared payload to immutable identities. Its reviewed workflow-level concurrency definition derives one shared non-secret group from the trusted repository ID, product/version domain, and verified pull-request number for both supported events; it deliberately excludes workflow name, run ID, attempt, config digest, session, and lineage so no supported route for the same PR can overlap. `workflow_run` name alone is not authority: the Host verifies repository ID, workflow file identity and exact supported concurrency definition, triggering run ID/attempt, event, associated PR, source/head repositories, current PR facts, trusted workflow source SHA, and token permissions before resolving state or provider secrets. No `pull_request_target` route is added. GitHub documents that a concurrency group permits at most one running workflow or job at a time in [Control the concurrency of workflows and jobs](https://docs.github.com/en/actions/how-tos/write-workflows/choose-when-workflows-run/control-workflow-concurrency).

## R4-D03: immutable reviewed snapshot

The C# Host obtains one authoritative pull-request record, freezes repository ID, pull-request number, base SHA, head SHA, base/head repositories, state, draft flag, and merge status, and then materializes the same-repository head tree by Git object SHA. It walks trees non-recursively so GitHub's recursive-tree truncation cannot be mistaken for completeness, accepts only regular blob modes, and rejects incomplete, duplicate, invalid, over-depth, over-count, or over-metadata results. The existing 20,000 tracked-file and 8 MiB metadata bounds remain authoritative.

Changed-file identity comes from the fully paginated pull-request files endpoint and is capped by the existing product limit of 200 changed files, below GitHub's documented 3,000-file endpoint maximum. Any incomplete pagination or exceeded product/platform limit returns an unsupported-size outcome; it does not review a prefix. The Host fetches exact base and head blobs and builds the bounded diff snapshot itself. GitHub-provided patch text is evidence only and is never the sole byte authority. Binary, submodule, symlink, missing, and over-bound content receives a deterministic unavailable classification rather than becoming a partial regular file. GitHub documents both the pull-file limit and paginated shape in the [pull requests REST API](https://docs.github.com/en/rest/pulls/pulls#list-pull-requests-files), and tree truncation behavior in the [Git trees REST API](https://docs.github.com/en/rest/git/trees#get-a-tree).

The tool root is a Host-created read-only materialization containing only validated regular blobs from the frozen head tree. It excludes `.git`, credentials, workflow scratch space, downloaded artifacts, wrapper files, payload files, symlinks, junctions, submodules, and special files. The Host re-fetches the PR immediately before the first GitHub mutation and requires the exact frozen head SHA. A head change observed before that barrier produces no publication and no state acceptance. Cross-API atomicity with a head change after the final read is not claimed; every public review and receipt names the exact reviewed SHA so a later run can supersede it safely.

Private-fork and public-fork snapshot support is deferred because R4 rejects fork-origin review before provider admission. A later fork-support decision must prove exact head-byte access without granting reviewed code secrets or execution.

## R4-D04: trusted configuration and policy

The direct `config-path` input defaults to `.github/agentic-pr-review.json`. The Host resolves the path only from the trusted workflow source commit, normally the default-branch commit that owns the privileged workflow run. It never reads configuration or instructions from the reviewed head, ambient checkout, mutable branch name, model output, trigger-workflow artifact, or cache.

The initial closed JSON shape is:

```json
{
  "schema": "agentic-pr-review.config.v1",
  "instructionsPath": ".github/agentic-pr-review/instructions.md",
  "publication": {
    "mode": "sticky",
    "inlineMinSeverity": "high"
  }
}
```

The exact allowed keys are `schema`, `instructionsPath`, and `publication`; the exact publication keys are `mode` and `inlineMinSeverity`. `mode` is `sticky` or `sticky_and_inline`. `inlineMinSeverity` is `high` or `critical`, defaults to `high`, and is rejected when an unknown value is supplied. The instruction path is one normalized repository-relative regular file under `.github/agentic-pr-review/`, with no symlink traversal, and is capped at 64 KiB of strict UTF-8. The complete config is capped at 16 KiB, rejects duplicate or unknown keys, and has no environment interpolation.

DeepSeek, model `deepseek-v4-flash`, the current adapter identity, thinking mode, the six-tool set, the central Agent limits, seven-day state retention, five-inline-comment cap, provider endpoint, prompt caching, and security policy are product-owned R4 constants rather than configuration fields. Test providers, arbitrary endpoints, raw-response capture, tool selection, generic budgets, state migration, and secret names are not public configuration. The exact trusted config and instruction bytes, source commit, provider/model/adapter, toolset, limits, and build identities participate in stable session scope.

## R4-D05: Action inputs, secrets, outputs, and completion presentation

The public direct-input allowlist is:

| Input                | Contract                                                                                              |
| -------------------- | ----------------------------------------------------------------------------------------------------- |
| `github-token`       | Required secret on supported trusted routes; distinct GitHub client only                              |
| `provider-api-key`   | Required DeepSeek secret; provider client only                                                        |
| `state-key`          | Required base64 encoding of exactly 32 random bytes; current write and read key                       |
| `previous-state-key` | Optional base64 encoding of exactly 32 previous bytes; read-only rotation window                      |
| `config-path`        | Optional trusted-source path; defaults as defined in R4-D04                                           |
| `pr-number`          | Required only for `workflow_dispatch`; rejected as an override when the event already supplies the PR |
| `state-mode`         | `auto` by default or `reset`; `reset` is accepted only on authorized `workflow_dispatch`              |

The wrapper obtains these through the Actions input mechanism, immediately masks every secret value, and passes typed bounded values to the Host without logging them. There is no runtime provider, model, endpoint, tool mode, artifact ID, state run ID, arbitrary session ID, raw instruction, payload path, debug, or limit input. The prepared-payload location is a private repository-proof seam and never appears in `action.yml`.

R4 publishes no stable Action outputs. The known historical downstream workflow does not consume the earlier outputs, and no current automation has demonstrated a decision that requires one. Reviewed SHA, publication URL, finding count, state action, and internal receipts appear only in a bounded step summary or diagnostics appropriate to their privacy class. A later stable output requires a named consumer, decision, stability/privacy contract, and migration behavior.

Exit zero covers `reviewed`, `reviewed_with_inline_warnings`, `skipped_untrusted_event`, `skipped_fork`, `skipped_draft`, and `skipped_closed`. Configuration, authorization ambiguity, missing required trusted-route credentials, snapshot incompleteness, provider failure, invalid Agent result, stale head observed before publication, state conflict, sticky publication failure, or unresolved outcome ambiguity fails the step. The wrapper renders one bounded summary and only Host-approved warning/error annotations; it never emits state plaintext, ciphertext, keys, provider content, tool results, raw exceptions, or untrusted strings as workflow commands.

The historical `v0.1.0` inputs and outputs are not aliases. The first post-removal release documents the breaking reset. No compatibility reader, converter, or dual surface is added.

## R4-D06: downstream-owned encrypted artifact store

The selected state class is `workflow-encrypted-completed-review`. Every downstream repository stores its own state through a built-in GitHub Actions artifact adapter. The repository's visibility controls platform visibility of artifact metadata and encrypted bytes. Public ciphertext exposure is acceptable; plaintext exposure is not.

The existing internal `IRestrictedStateStore` becomes an asynchronous transport-neutral opaque-snapshot seam. Its adapter operations are limited to complete bounded listing by an opaque exact name, metadata/provenance read, encrypted-byte download, immutable upload, exact readback, and authorized physical deletion. `RestrictedStateService` remains the sole owner of workflow authorization, state-key resolution, opaque-name derivation, encryption/authentication, SESSION admission, scope, accepted lineage, prepare/reconcile/accept, expiry, reset, and stable outcome mapping. A shared adapter conformance suite is the internal extension contract; the interface is not a public binary ABI.

Before any scoped state lookup, the Host completely enumerates the fixed generic artifact name `agentic-pr-review-state-root-v1`. Each matching artifact contains one authenticated encrypted `LocatorRootV1` sentinel and nothing executable. The public constant name reveals only that the repository has used this product; it carries no repository, workflow, pull-request, session, provider, model, policy, or lineage mapping. A first initialization is authoritative only when complete bounded enumeration proves that no sentinel record exists and the platform retention invariant below is available. It derives the initial 256-bit locator root from the current state key under a dedicated one-time initialization KDF domain, uploads generation zero, and requires exact readback. Concurrent first initializers supplied the same key therefore propose semantically identical roots rather than silently creating unrelated stores.

The live sentinel family is capped at eight artifacts. Every decrypted sentinel binds its locator root, unsigned 64-bit generation, current key identifier, exact predecessor artifact IDs and archive/envelope digests, exact superseded artifact IDs and digests, creation time, and platform expiry. Complete enumeration over the fixed name selects one maximal authenticated successor chain. Same-root records with identical generation, key identifier, predecessor set, and supersession set are equivalent duplicates and count as one logical head; they are pruned to one physical record. Distinct roots, incomparable successors, overflow beyond eight live artifacts, incomplete pagination, an unknown unauthenticated artifact not named by the selected head's authenticated supersession set, or uncertain readback is `state_locator_conflict` before scoped listing, SESSION admission, provider work, or publication.

Rotation authenticates the unique live head with the current or authorized previous key, appends generation N+1 encrypted under the current key with the same root, and binds the exact identities and digests of every live N-or-earlier sentinel as superseded. After exact readback and complete re-enumeration have selected that current-key head, the Host deletes each superseded artifact by exact ID and verifies that only the chosen head or equivalent same-head duplicates remain. A later run may ignore an undecryptable older artifact only when the authenticated current head names its exact ID and digests as superseded; otherwise it fails. The previous key may be removed only after the current-key head is established and no unexpired state or transaction record still requires the previous key. An interrupted rotation, deletion ambiguity, or competing distinct successor remains fail-closed and retryable from the same authenticated head.

Sentinel refresh uses the same successor/supersession/pruning protocol and occurs only when rotation is requested or the selected head's platform expiry is not later than every dependent scoped artifact's logical and proven platform expiry by at least 24 hours. Logical state and transaction retention remains bounded to seven days, but platform retention requests intentionally include transport margin: candidate, lineage-head, intent, acceptance, reset, and cleanup artifacts request at least eight days and the sentinel requests at least ten days. Requested day counts are never proof. After every scoped upload, returned `expires_at` metadata must prove both the record's required logical window and the sentinel's 24-hour margin; the Host refreshes and re-enumerates the sentinel before the scoped record becomes eligible, or returns `state_locator_unavailable`. Under that enforced invariant, complete sentinel absence means no dependent scoped record can remain live and permits initialization. If the invariant cannot be established, absence is not bootstrap. Out-of-band administrative deletion is unsupported destructive state loss, not a product reset path.

The Host derives lineage-head, candidate, publication-intent, acceptance, reset, and cleanup artifact names from a domain-separated HMAC keyed by the stable locator root over the base scope and object class. These exact names never depend on the unknown lineage epoch or derived session ID; the encrypted objects bind both. Encryption and authentication of every new record use the current state key. Key rotation therefore changes ciphertext/key identity without changing locator or lineage discovery authority. Names expose no raw scope fields. Every artifact contains one bounded encrypted object and nothing executable. The wrapper sees only the opaque or fixed generic name, bounded file path, expected digest, retention request, and operation result; it cannot decrypt, derive scoped locators, or choose lineage.

Each created artifact is an immutable append record. The bridge never uses `overwrite: true`; same-name success, collision, or rejection is not a repository-wide lock or conditional-create primitive. A prepare uploads one candidate bound to the observed accepted predecessor. A run owns publication only when a complete enumeration finds its candidate as the sole valid next child of that predecessor and finds no accepted successor; zero, multiple, incomplete, or ambiguous candidates mean no owner and no publication. After verified sticky publication, acceptance uploads a separate encrypted receipt that binds candidate identity, predecessor, exact reviewed SHA, publication receipt, producing run ID/attempt, and trusted timestamp. The Host reports acceptance success only after another complete enumeration finds its exact receipt as the sole valid accepted successor of the expected predecessor. Multiple valid candidates or acceptance successors are `state_conflict`; none silently replaces the predecessor. The prior accepted lineage remains current until conflict cleanup or explicit reset. The verified shared per-PR workflow concurrency group is the authoritative exclusion primitive that prevents another supported run from admitting a candidate between the final ownership check and sticky write. The Host's sole-candidate rule, predecessor check, unique-successor rule, exact readback, and conflict outcome remain authoritative validation and recovery rules inside that serialized route and against artifacts from interrupted or unsupported activity.

This is CAS-equivalent safety over immutable append records, not a claim that GitHub artifacts provide a transactional mutable row, repository-wide conditional create, or cross-run name lock. Outcome-unknown upload is reconciled only by exact opaque name, artifact ID, producing run identity, platform archive digest, project encrypted-envelope digest, and authenticated receipt. The archive digest and raw envelope digest are distinct identities unless the bridge proves byte equivalence. Incomplete pagination, delayed or ambiguous readback, duplicate identity, expired download, missing digest, or deletion ambiguity is a non-success result. No operation assumes that absence in a partial list is authoritative.

The service retains the accepted generation and immediate predecessor for seven days from Host-trusted acceptance. Pending candidate `prepared_at` is not `accepted_at`: immediately after exact sticky readback, the Host reads its trusted clock once, binds that value into the acceptance receipt, and sets logical expiry to exactly `accepted_at + 604800` seconds. Before the first sticky request, candidate metadata must prove platform coverage through the current trusted time plus the seven-day logical window and the complete bounded sticky request/readback budget. After sticky readback, acceptance is forbidden unless the candidate's returned `expires_at` still covers the exact logical expiry and the active lineage-head metadata covers the same window. The Action therefore requests at least eight days of platform retention for every accepted-generation backing candidate and refreshes the active lineage head when needed; repository or metadata limits that cannot prove coverage fail without accepting shortened state. Expired state is ignored during selection, and expired candidates, acceptance receipts, and superseded predecessors are deleted when the `actions: write` token permits. Repository retention may be longer; logical expiry is authoritative. GitHub documents artifact list/get/download/delete behavior, public-resource reads, and `Actions` write permission for deletion in the [Actions artifacts REST API](https://docs.github.com/en/rest/actions/artifacts). `actions/upload-artifact` documents v4 immutable uploads and returned artifact digest in its [official repository](https://github.com/actions/upload-artifact).

GitHub Actions cache is rejected because its sharing and poisoning properties are designed for dependency reuse rather than accepted lineage. Git refs are rejected for encrypted session payloads. The adapter never restores trigger-workflow artifacts or caches into the privileged execution root.

R4 has no arbitrary dynamically loaded .NET adapter assembly, user-provided adapter type, or stable HTTP/sidecar protocol. A future maintained Supabase adapter should first prove Postgres transactional RPC, Row Level Security, credential federation, retention, deletion, and operational ownership. Raw Supabase Storage is not presumed to provide compare-and-swap. Only after a second real adapter demonstrates a common boundary may the project consider a narrow opaque-state external protocol. Supabase documents [Postgres Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security); its current [third-party authentication overview](https://supabase.com/docs/guides/auth/third-party/overview) does not by itself establish a GitHub Actions OIDC route.

## R4-D07: key provisioning and rotation

`state-key` receives the downstream secret convention `AGENTIC_PR_REVIEW_STATE_KEY`, containing base64 for exactly 32 random bytes. The Host derives the non-secret key identifier as a domain-separated SHA-256 digest of the decoded key; users do not type an independent key ID. `previous-state-key` receives `AGENTIC_PR_REVIEW_PREVIOUS_STATE_KEY`, when supplied, and is read-only for every unexpired locator-root, lineage-head, candidate, publication-intent, failure/abandonment, acceptance, reset, cleanup, current, or predecessor record needed to reconcile the old-key lineage. Every new sentinel, head, candidate, intent, receipt, or reset record is encrypted with the current key. Successful rotation first establishes and prunes the current-key locator-root head, then lets the next accepted generation rotate state and pending records through normal immutable successors; R4 does not rewrite artifacts in place.

The state key is separate from the provider key, `GITHUB_TOKEN`, payload identity, artifact bytes, and the persisted locator root. There is no repository-derived, provider-derived, or plaintext fallback. Authorization and event/source verification occur before decoding or resolving key inputs. Missing or malformed current key fails the supported trusted route before sentinel listing or provider construction. If any sentinel exists but neither the current nor previous key authenticates the unique live root, the result is key-unavailable or authentication failure, never first-use bootstrap. Wrong-current/no-previous, wrong-current-and-previous, lost-key, revoked-key, multiple-root, and cross-scope cases fail before SESSION admission; successful current-plus-previous rotation preserves one root. Temporary decoded keys and locator-root buffers are cleared where the runtime permits.

The repository owner creates and rotates secrets outside R4 code. Selecting these prospective names is documentation authority only; this design authorizes neither a repository/organization/environment secret mutation nor a protected environment. The production setup documentation must explain current-plus-previous rotation, the sentinel successor/readback/pruning gate, and removal only after no unexpired old-key state or transaction record remains. Fork and unsupported event routes receive neither key.

## R4-D08: scope, session, reset, and retention

Base scope contains repository ID, trusted workflow file identity, trusted source commit identity, pull-request number, provider/model/adapter, exact config and instruction digest, toolset digest, limits digest, and payload build identity. Current base/head SHAs remain transition facts and encrypted provenance rather than base scope. The Host derives one exact opaque lineage-head artifact name from the locator root, base scope, and `lineage-head/v1`, before it knows an epoch or session ID. Candidate, intent, acceptance, and reset names are likewise base-scope/class locators. Every record binds the selected epoch, derived session ID, base scope digest, predecessor, and object identity inside authenticated ciphertext.

Before provider execution, complete lineage-head enumeration selects the active epoch. The exact-name family is capped at eight physical artifacts and retains the active semantic head plus its immediate predecessor for the review-state window; equivalent duplicate heads are idempotent and pruned, while overflow or incomparable successors conflict. Zero valid head claims plus no current-format state permits one initial claim whose opaque epoch is deterministically derived from the locator root and base scope under `initial-epoch/v1`; concurrent equivalent claims are idempotent. One unique valid initial, reset, or expiry successor supplies the epoch, after which the Host derives the session ID from base scope plus epoch. Multiple distinct initial epochs, competing successors, incomplete enumeration, or a head/state mismatch is conflict.

An authorized reset receipt is the only manual transition from an existing epoch to a new keyed-pseudorandom epoch derived from the prior head and authorized workflow run/attempt; an exact retry proposes the same epoch. Different reset requests that race become competing successors and neither may publish or accept. Verified expiry cleanup appends an `ExpiryTransitionV1` whose next epoch is keyed-deterministic over the prior head, expired acceptance identity, and logical expiry boundary; concurrent exact expiry transitions are equivalent, while any mismatch conflicts. Reset and expiry successors authenticate the prior head, transition reason, cleanup evidence, and new epoch. Older superseded head records are pruned only after exact successor readback.

Different workflows, configurations, or accepted reset epochs reviewing the same pull request have independent lineages. There is no caller-supplied session ID, and no process generates an epoch before enumerating the epoch-independent lineage-head authority.

`auto` follows this normative transition table:

| Observed state after complete authenticated selection                                                                    | Automatic behavior                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| No current-format state family or lineage-head claim exists                                                              | Observable bootstrap with the deterministic initial epoch claim and derived session ID                  |
| Only a Host-classified non-current historical family exists                                                              | Observable bootstrap; the historical family is never selected as current                                |
| Authenticated current exists but is expired, and verified cleanup plus head transition proves true absence               | Observable bootstrap under the next unique epoch                                                        |
| Selected-current state is malformed, unauthenticated, scope-incompatible, continuation-invalid, or missing required data | Fail closed before Agent/provider admission                                                             |
| Unique selected-current state matches the same reviewed head                                                             | Restore                                                                                                 |
| Host verifies that the new head descends from the accepted prior head and the base transition satisfies SESSION rules    | Restore and continue the same lineage                                                                   |
| Accepted current exists but ancestry is unknown, diverged, unrelated, or force-pushed                                    | Fail closed; require authorized `state-mode=reset` or a separately reviewed future new-scope transition |
| Enumeration, current selection, locator root, or accepted lineage is incomplete, multiple, or ambiguous                  | Fail closed                                                                                             |

An incompatible selected-current record is never reclassified as absent or non-current merely to permit bootstrap.

`reset` is an authorized `workflow_dispatch` operation. It completely enumerates and compare-deletes every candidate, intent, acceptance, failure/abandonment, and cleanup record in the active epoch while preserving the repository-level locator-root sentinel and bounded lineage-head history, verifies scoped absence, appends the unique authenticated reset successor for the prior head, and derives the new session ID only after exact receipt readback and complete head re-enumeration. The former accepted lineage remains non-current retention evidence until deletion is verified; it cannot be replayed into the new epoch. Any deletion, competing reset successor, receipt, or enumeration ambiguity fails without starting the new root. A PR event, config value, model output, possession of ciphertext, or possession of a key alone cannot request reset. Seven-day logical retention is fixed for R4 and is not a public configuration field.

## R4-D09: deterministic publication

The sticky comment is the complete public review. It is enabled for every successful review, including an empty result, and is capped at 50,000 characters after the Agent's existing 20-finding bound. The body names the exact reviewed head, renders only validated grounded findings, states truncation explicitly, and ends with one R4 marker containing an opaque publication-scope digest, body digest, and reviewed head. The Host updates only the unique exact R4 marker for its publication scope. It does not adopt historical `agentic-pr-review:v1` or M4 state markers; multiple matching R4 markers fail closed before write.

Finding fingerprints are domain-separated digests over the validated severity, title, message, and ordered grounded evidence projection. They exclude state IDs, run IDs, model prose outside the admitted finding, and mutable comment IDs. Inline keys add the exact path and mapped right-side line. Existing exact R4 inline keys suppress duplicates.

Inline publication is off by default. In `sticky_and_inline` mode the Host considers at most five high-or-critical findings that map exactly to current right-side diff lines. A valid grounded finding that cannot map remains sticky-only. The Host attempts one batch review, re-lists before any documented individual fallback, and never hides the sticky review because an inline operation fails. Partial inline failure produces `reviewed_with_inline_warnings` after accepted state and records only bounded reason counts.

Sticky create/update and inline writes require exact API response validation plus readback. Network failure after a request is `outcome_unknown` until exact marker/body reconciliation succeeds. Empty review updates the sticky body to an explicit no-actionable-findings result rather than deleting history. All renderer text is escaped or emitted as inert Markdown; untrusted content never becomes a workflow command.

## R4-D10: publication, state, and concurrency transaction

The authoritative sequence is:

1. verify that the trusted workflow source contains the exact authoritative shared per-PR concurrency contract, then resolve the bounded locator-root sentinel successor chain and immutable configuration, build the exact snapshot, enumerate the epoch-independent lineage-head authority, and restore the unique accepted predecessor in the selected epoch;
2. before any Agent/provider call, completely enumerate and authenticate pending candidates, publication intents, failure/abandonment records, and acceptance receipts at their base-scope/class locators for that epoch and predecessor, then run the reconciliation matrix below;
3. only when reconciliation proves that no pending transaction owns work, run the Agent/provider and validate the terminal result, candidate state, complete sticky publication payload, marker, and body digest;
4. re-fetch the PR and require the exact reviewed head;
5. immutably upload the encrypted candidate containing or authenticating the complete validated publication payload, reconcile an unknown upload by exact digest and provenance, completely re-enumerate, and proceed only when this candidate is the sole valid next child and therefore the publication owner;
6. append and read back an encrypted publication-intent receipt before the first sticky request, then re-fetch the PR, require the exact reviewed head and unchanged publication ownership, and prove from returned artifact metadata that the candidate, active lineage head, and locator sentinel cover the bounded publication interval and full post-acceptance logical window;
7. create or update the R4 sticky comment and verify the exact marker, reviewed head, and body digest by readback;
8. re-fetch the PR again, require the exact reviewed head, completely recheck that the same candidate still owns publication with no accepted successor, and upload the encrypted acceptance receipt using the observed predecessor;
9. completely re-enumerate and report accepted only when this receipt is the unique valid successor; reconcile unknown acceptance without re-encrypting, republishing, or invoking the provider;
10. optionally publish deduplicated inline comments and report bounded partial warnings;
11. prune expired or safely abandoned artifacts without changing the known committed outcome.

The provider-before-work reconciliation matrix is normative:

| Pending classification                                                                                 | Required behavior before any provider call                                                                                                                                                      |
| ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Exact unique acceptance receipt exists                                                                 | Reconcile its candidate and sticky readback, then return the known committed result                                                                                                             |
| Sole candidate and exact matching sticky marker/body exist                                             | Recheck the exact head and sole ownership, complete acceptance, and return without another provider or sticky write                                                                             |
| Sole candidate exists with no publication-intent receipt and exact marker absence is completely proven | Resume from the stored validated publication payload; append/read back intent before the first sticky request                                                                                   |
| Publication intent exists but the exact marker/body is absent after bounded complete readback          | Classify sticky outcome as unknown and fail closed; do not call the provider, repeat the sticky write, accept, delete the candidate, or start another child                                     |
| A known-not-written publication failure receipt exists                                                 | Resume publication from the stored validated payload without another provider call, subject to exact-head and ownership checks                                                                  |
| Multiple, incomplete, unauthenticated, or ambiguous candidates/intents/receipts exist                  | Return conflict without a provider call or GitHub mutation                                                                                                                                      |
| A candidate is bound to a stale head                                                                   | Abandon/delete only when there is no intent and complete readback proves no sticky write crossed; otherwise retain a bounded conflict until explicit reset, verified cleanup, or logical expiry |

A sticky success followed by unknown acceptance is recovered from the exact public marker plus encrypted candidate/receipt; a retry must not duplicate the sticky comment or call the provider merely to discover the previous outcome. Acceptance never precedes sticky readback, so the product does not advance continuation past an unpublished complete review. Inline failure cannot invalidate accepted state because the sticky comment already carries the complete result. The conformance suite injects crash or cancellation after candidate upload, intent upload, sticky request, sticky readback, acceptance upload, acceptance readback, and cleanup, and proves the matrix at the next process start.

If competing candidates share a predecessor, only a unique verified acceptance successor can advance. Multiple successors, lost workflow ownership, changed head observed at either barrier, partial enumeration, or ambiguous mutation fails closed and leaves the prior lineage current. The supported workflow definition uses the same repository-and-PR concurrency group across `workflow_run` and `workflow_dispatch`, with `cancel-in-progress: false`, and GitHub's one-running-run guarantee is required for side-effect safety. The Host still rejects an unverified group definition and rechecks exact head, candidate ownership, receipts, and readback; those checks detect stale or externally introduced records but are not presented as an atomic artifact lock.

Cancellation before the first external write cancels normally. After an upload or comment request may have crossed a commit boundary, bounded reconciliation ignores caller cancellation long enough to classify the outcome. Once acceptance is known, the result cannot claim cancellation or rollback; cleanup and inline warnings are post-commit outcomes.

## R4-D11: launcher, result, and artifact bridge

The wrapper and Host ship atomically and perform a build-identity handshake before any privileged operation. Raw wrapper-to-Host facts are limited to exact event JSON path and digest, repository/run/workflow identifiers, trusted source/payload identity, bounded Action inputs, secret channels, cancellation, and a private artifact bridge endpoint. The Host-to-wrapper result is a closed status, exit class, bounded summary model, bounded annotations, and optional private artifact commands. Neither direction carries a review-domain SESSION format.

The private artifact bridge supports only exact-name list, metadata, download, immutable upload, readback, and delete operations with fixed size/count/time limits. A request carries an opaque name, local bounded path or destination, expected digest, retention request, and correlation ID. The wrapper rejects path escape, symlink/reparse traversal, unknown operations, duplicate correlation IDs, response overflow, and build mismatch. The Host chooses every logical operation and validates every result; the wrapper never opens encrypted state as a SESSION object.

The exact framing, file names, DTO names, and standard-stream or private-pipe mechanism belong to the focused launcher leaf, provided the seam remains private, closed, bounded, cancellation-aware, and atomic with the bundle. It has one build discriminator for mismatch rejection, not an independently supported protocol version family. The bridge is not the future adapter-extension interface.

Within pre-1.0 R4 work, "freeze" establishes this contract as the canonical integration baseline and single ownership point; it is not a hard compatibility freeze. A later focused issue may revise the private contract directly when the delta is necessary to that issue's scope, all affected in-tree consumers and tests are updated atomically, and concurrent shared-contract work receives an explicit ownership handoff. Such a revision does not require reopening the original contract issue, creating a separate amendment issue, retaining compatibility readers, or introducing another protocol version family. Split the change into a separate design or implementation issue only when it becomes an independent product or security decision, crosses a durable compatibility boundary, or no longer fits one focused PR. Before the first supported post-reset Action release, prospective public inputs and outcome semantics may likewise be corrected without historical aliases or a dual surface, while any already supported public or durable boundary still follows the versioning and migration policy.

Secret values, provider requests/responses, SESSION plaintext, tool data, findings before public validation, state keys, decrypted artifacts, arbitrary environment dictionaries, ambient paths, and GitHub client objects never cross into wrapper presentation or Agent/model-visible objects. The wrapper registers masks before launch and exposes only the minimum child environment.

## R4-D12: migration and deletion disposition

| Current family                                                                                | R4 disposition                                                                                              | Last responsible deletion gate                                                                                 |
| --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| C# `Agent/**`, tools, SESSION, DeepSeek adapter, restricted envelope, local conformance store | Surviving target behavior; compose into ActionHost and migrate the store seam asynchronously                | Never deleted by R4 except replacement-local refactors with conformance parity                                 |
| C# `Ledger/**`, `Prefix/**`, `Protocol/**`, deterministic CLI/application paths               | Replacement evidence for stable validation, canonicalization, process, and publisher behavior               | Delete only after the owning C# Host/state/publisher replacement test passes in framework and Native AOT modes |
| `src/runtime-invocation/**` and `src/live-runtime-invocation/**`                              | Replaced by the C# Host plus private launcher contract                                                      | Removed by W3 after end-to-end Host launch, cancellation, result, privacy, and build-identity proofs passed    |
| `src/live-provider/**`                                                                        | Replaced by the C# DeepSeek request, transport, parser, Host transaction, and canary evidence               | Removed by W4 after the retained C# route and exact no-second-route oracles passed                             |
| removed `src/state-v2/**`, its schema, fixtures, and generators                               | R4-W5 retired StateV2; no current reader or compatibility surface.                                          | Replaced by R4 Host W5 after mapped S5/S6/P5/P6/E1 evidence passed                                             |
| removed `src/comments.ts` and `src/comments.test.ts`                                          | Sticky rendering, marker, fingerprint, discovery, mutation/readback, and recovery evidence is checked in C# | Removed by W8 after P1/P2/P5/P6 and E1 sticky/recovery coverage passed; old M4 markers remain non-adoptable    |
| removed `src/inline-comments.ts`, `src/target.ts`, and their tests                            | Inline admission, target, duplicate, fallback, readback, and warning evidence is checked in C#              | Removed by W9 after H2/H5/P3/P4/P6 and E1 inline, warning, and stale-head coverage passed                      |
| Removed TypeScript review protocol DTOs/validators/adapters and structured assembly           | Historical W10 evidence; embedded schemas and the mixed fixture corpus remain live C# contracts             | Removed by W10 after exact disposition, retained-corpus, protocol, and E1 route proof passed                   |
| Removed root `src/types.ts` and `src/utils.ts`                                                | W15 exact 52-symbol disposition plus W7-W10 path/symbol/SHA import provenance                               | Removed by W15 after all root consumers closed; direct-runtime and ActionHost owners remain explicit           |
| Permanent residual-reference rules and S2 artifact-provenance vectors                         | Closed conformance, governing, historical, and negative-evidence inventory                                  | W13 owns the terminal audit; any later replacement must preserve the named behavior and owner                  |
| Historical `v0.1.0` tag and docs                                                              | Immutable historical release evidence                                                                       | Never rewrite; document the breaking R4/R7 migration                                                           |
| Nested Action metadata, generated bundle, and `dist:check` guards                             | Current repository-controlled prepared-payload proof boundary                                               | R7 alone may add release download, a root alias, stable outputs, or public-default support                     |

`src/ledger-csharp.ts` and its root tests were removed by R4-W7 after the P6/S6/E1 replacement map and framework proof passed. The pinned E1 replacement record and base inventory remain the deletion evidence. `src/live-provider/**` was removed by R4-W4 after its current request, privacy, failure, transaction, canary, and route-uniqueness protections were dispositioned to the retained C# DeepSeek/ActionHost path or their later W11/W12/W14 owners; superseded M4 request-shape details were not recreated. `src/state-acceptance/**` and its dedicated schemas were removed by R4-W6 after the S1-S6/P5/P6/E1 replacement map proved encrypted artifact, stale-authority, readback, cleanup, and publication-recovery behavior; Git-ref/CAS/lock/counter and selector-before-sticky mechanics remain historical only. `src/comments.ts` and `src/comments.test.ts` were removed by R4-W8 after named P1/P2/P5/P6 replacement vectors and the E1 sticky/recovery scenarios passed; the historical base inventory and negative marker-adoption tests remain evidence. `src/inline-comments.ts`, `src/target.ts`, and their root tests were removed by R4-W9 after all material assertions were classified in the checked replacement ledger: H2/H5 retained admission and exact-diff authority, P3 retained deterministic candidate mapping and identity, P4 retained bounded deduplication, batch, fallback, readback, and head barriers, and P6/E1 retained post-acceptance warning-only behavior. The old patch-hash/delta algorithm, state-keyed marker, confidence/configurable-cap behavior, generic 5xx fallback, and 3,000-entry limits were deliberately superseded rather than recreated. `src/protocol/**`, `src/structured.ts`, and `src/structured.test.ts` were removed by R4-W10 after the checked disposition ledger, exact retained-corpus digest, C# protocol/application tests, and E1 `dispatch-continuation`/`inline`/`public-result` route proof passed. The three embedded review schemas and complete mixed `protocol/fixtures/v1/**` corpus remain live internal C# contracts; pathless direct-runtime results remain valid while ActionHost findings require grounding, and the old TypeScript mapper, Ajv diagnostics, caps/filtering, and 16-hex fingerprint were deliberately superseded without a converter or public compatibility claim. W15 then removed root `src/types.ts` and `src/utils.ts` after checking every exported symbol exactly once and binding the fourteen historical W7-W10 consumers to their E1-base paths, symbols, and SHA-256 values. Its record distinguishes retained direct-runtime schema/model behavior from the deliberately different ActionHost H3/H5/Agent/publisher contracts; no compatibility barrel or generic utility replacement remains. `src/prefix-contract/**` and its regeneration script were removed by R4-W11 after the immutable corpus, C# manifest and golden-vector consumers, Ledger digest known answer, and E1 continuation proof were pinned; historical generator and TypeScript diagnostic strings remain only inside immutable evidence. `src/provider-metadata/**`, its schema resource, and dedicated fixture corpus were removed by R4-W12 after the retained C# DeepSeek/ActionHost identity, privacy, continuation, cache, and failure evidence passed. W14 removed the TypeScript canonicalization implementation and its tests after closing every RFC 8785 or JavaScript-only assertion in the checked receipt. R4-W14 retired the TypeScript canonical-json family; C# Canonical remains current and the prefix corpus remains immutable evidence. W5's opaque StateV2 sidecar bytes, descriptors, hashes, and fixtures were removed with the retired representation rather than retained by a current owner.

R4-E2 ([#179](https://github.com/SolusQuest/agentic-pr-review/issues/179)) remains the exclusive owner of the complete post-W13 ActionHost Linux x64 Native AOT proof and final deleted-route receipt. W11 supplies source deletion, retained C# corpus evidence, and E1 framework proof only; existing AOT regressions do not claim E2 completion.

R4-E2P ([#216](https://github.com/SolusQuest/agentic-pr-review/issues/216)) is the direct private prerequisite between E2 and E3. It owns the single compiled final E3 workflow topology, the proof-only `linux-x64` production payload with production GitHub/state/publisher owners and an in-process deterministic DeepSeek transport, proof-control and stale-window synchronization, exact trusted policy bytes, and the offline two-root preparation/supplemental-receipt contract. Its separate Native AOT verifier executes the actual Host, deterministic provider, control service, and stale coordinator with verifier-only synthetic outer dependencies; the production payload itself runs exact fail-before-network smoke and has no synthetic transport selector. The receipt binds both binaries and their shared managed payload identity while explicitly recording that standalone default-GitHub success remains E4-owned. Its proof role is `r4-e2p`, while both sides of the W2 launch handshake remain `r4-w2`. It does not add the checked E3 workflow, execute a credential-bearing operation, or create release delivery. E3 [#180](https://github.com/SolusQuest/agentic-pr-review/issues/180) instantiates the receipted topology and prepares only the production payload without changing Runtime code, building the verifier, or rebuilding the payload outside the frozen preparation contract.

The staged E2P successor keeps the #216 Action source at `5b5769753653bb3fd3e68cf8b7bb88a1bd350613` and keeps the public `r4-w2` launch ABI unchanged. A private compile-time payload identity separately binds the descendant payload source commit and tree. Default Runtime composition remains v1. Every descendant `ActionHostTrustedProofPayload` build requires both exact payload source identities and is unconditionally v2; missing either identity fails before compilation and never falls back to v1. Retained v1 payload evidence is built only from detached exact #216 source. The source identity values authenticate the build and do not select its version. The v2 workflow renders independent Action source, payload source, and payload digest values, and its proof-specific final admission repeats the exact `main` base ref and workflow-SHA base binding before Host side effects. The staged receipt treats `source_commit` and `source_tree` as payload-source identity, permits neither conflation with the pinned Action source nor runtime format negotiation, and binds deterministic Native AOT payload/verifier/build identities plus successful execution of the complete synthetic Host/control/stale/artifact route. That synthetic lifecycle remains test-local route evidence: it does not freeze E4 live/transient counts, terminal category membership, or object identities. The dependent evidence issue owns two separate production-path layers: an expected success-role contract of exactly seven product anchors, and an authenticated observed cleanup inventory containing every actually observed operation-owned resource. Only an exact seven-role match may complete or project publicly; separately authorized recovery cleanup may include authenticated operation-owned extras, while cross-operation or ownership-ambiguous resources remain non-deletable.

E3 owns the inert exact-rendered workflow, the active descendant v2 receipt plus the explicitly historical #216 v1 baseline, same-repository normal and stale fixture manifests, and separate host-restricted/public-safe evidence contracts. Its static checker owns the closed repository-wide R3/R4 secret and proof-control route union and rejects any alternate proof-scoped workflow route, including `github.token` routes. Its offline contract binds exact Action/payload identities, exact `main` base admission, three attempt-1 pending-to-approved protected-job transitions, two distinct completely paginated concurrency-group observations, the normal two-comment and stale four-comment families, the production-derived exact-seven success inventory, the separate 15-record cleanup inventory, three non-substitutable authorizations, ordered cleanup/readback gates, live/cross-sink/public-scan canaries, and the approved restricted package before emitting the nonjoinable public representation. The environment approver may be the triggering actor in the repository's single-maintainer topology; the contract pins `prevent_self_review` to `false` and forbids administrator bypass. E4 [#181](https://github.com/SolusQuest/agentic-pr-review/issues/181) remains the only owner of obtaining live readbacks, temporary authorization, fixture creation, dispatch, barrier release, cleanup mutation, host-restricted evidence retention, and publication of a real public-safe evidence instance.

The executable E3 evidence boundary is split by capability. A GET-only capture executable accepts one bounded token file under an approved marked host-restricted root, records completely paginated safe response bodies/header digests, admits each downloaded artifact through the production artifact-envelope limits before `CreateNew` persistence, and removes the local token copy after its last use. The root, marker, credentials, retained sources, archives, encrypted objects, and manifest are reopened through no-follow handles with same-volume, single-link, owner-access, stable physical-file-identity, digest, and size checks. A separate no-network production-codec oracle accepts only the capture manifest and current/optional previous key files, classifies the encrypted outer state into the exact seven success roles plus the complete observed cleanup inventory, writes only outer metadata and payload digests, and removes the local key copies. The oracle builder resolves the authorized commit and tree through Git objects, materializes only regular blobs into a fresh read-only snapshot, builds with fresh isolated restore/intermediate/output roots, and re-enumerates and rehashes the leased snapshot after compilation. Its output binds the compile-time source commit/tree and SHA-256 identities of both the executing oracle assembly and the production Runtime assembly. The offline assembler accepts the checked source map plus an ordered reference-only source bundle, reconstructs the host document from raw captures, production-codec output, receipts, attestations, and cleanup readbacks, and binds the finalized capture manifest, oracle result, credential-absence readbacks, and public projector without making a network call. It consumes one bounded canonical scan-only stdin frame containing the actual protected credential/state/provider/tool classes and canaries; those bytes never enter command arguments, environment variables, files, digests, or the JavaScript child and are zeroed after the final scan. Every API-derived source-bundle entry references exact finalized capture-manifest source IDs and raw-body digests; authorization and proof-control body digests remain separate from those raw response digests. Receipt, UI, oracle-build, cleanup-plan, leak-scan, and restricted-package inputs bind their own checked or retained digests. The assembler accepts no caller-authored complete host document, caller-supplied source value, or unreferenced otherwise-valid subdocument. Live canaries and the exact success/cleanup inventories are derived from the capture, proof-control, and codec authority chain rather than retained as asserted values. Recovery-only authenticated extras may enter the canonical cleanup plan and private host package but prohibit this assembler from producing a public projection.

The checked entry points are `ActionHostTrustedProofCapture`, `ActionHostTrustedProofEvidenceOracle`, `ActionHostTrustedProofEvidenceAssembler`, `scripts/generate-r4-trusted-proof-cleanup-plan.mjs`, and `scripts/project-r4-trusted-proof-evidence.mjs`; the JavaScript assembly module is invoked only behind the assembler boundary. Credential arguments name only the exact `github-token`, `current-state-key`, and optional `previous-state-key` files beneath the marked root; no credential value is accepted on the command line. The capture plan is pinned to the checked source-map digest, artifact metadata pagination stays within the exact producing-run endpoint family, and the C# assembler holds no-follow read leases for every retained source body, archive, and encrypted object while the JavaScript derivation runs. Before derivation it pins the exact repository, worktree, and public-log corpus. JavaScript stages the exact digest-bound candidate only under the restricted root. C# then scans that candidate and the leased corpus for category-specific fragments, current Host operation identities, encrypted objects and archives, and exact protected documents; reopens and re-enumerates the complete corpus; and only then creates the public file without overwrite. A final readback and corpus enumeration permits only that bound public-file addition. On Windows those leases combine owner-only ACL admission with read-only sharing so replacement, deletion, and mutation remain denied for the whole assembly window. The boundary then revalidates every leased physical identity, credential-copy absence, canonical Host and private-manifest readbacks, and the exact public candidate digest. Host and private files are create-new and reopen through the restricted root; only exact-seven success may create the post-cleanup public projection, also without overwrite.

E4 uses three non-substitutable maintainer checkpoints. Setup authorization covers only the precomputed environment baseline, two fixture heads/PRs, their secret-free CI/inert preflights, and bounded rollback. Execution authorization is recorded only after the concrete PR, ref, head, base, manifest, destination, actor, credential-input, and command coordinates exist. Cleanup authorization is recorded only after all runs are terminal and the authenticated inventory has produced a byte-stable concrete plan digest; no destructive cleanup begins before that exact digest is approved. The projection gate follows ordered control, state, authorization/secret, environment, fixture, sticky, local-credential, and private-manifest readbacks.

The checked [`r3-single-shot-removal-handoff.md`](./r3-single-shot-removal-handoff.md) remains the path-level starting inventory. Every deletion PR refreshes live imports, scripts, workflows, fixtures, public docs, downstream pins, and residual allowlists; a missing R3 call site is not sufficient deletion evidence, and a test-only import is not sufficient retention evidence.

No superseded pre-rebaseline state conversion, compatibility reader, dual writer, input alias, comment adoption, or parallel public runtime is added. Host-classified non-current historical state may bootstrap observably under the current SESSION rule. Any selected-current authentication, structure, scope, continuation, or ancestry failure remains unsupported and fails closed unless an authorized explicit reset completes.

## R4-D13: proof layers

R4 uses three separate proof layers.

1. Deterministic conformance covers Host authorization, exact supported workflow-concurrency-definition admission, snapshot traversal and diff construction, config resolution, the internal state-store suite, artifact pagination and ambiguity, bounded locator-root first initialization/rotation/refresh/supersession/pruning/retention, accepted-at versus artifact-`expires_at` coverage, wrong/lost/revoked-key failures, old-key pending-record reconciliation, concurrent initial epoch claims, crash after epoch claim, reset/expiry retry, competing reset or expiry successors, next-process epoch discovery, selected-current transition failures, publisher rendering/readback, every transaction crash cut, wrapper framing, cancellation, canaries, and retained replacement oracles. PR and push CI use only synthetic providers and credentials.
2. Framework-dependent integration and the separate `linux-x64` Native AOT E2P verifier execute the complete Host/Agent/DeepSeek-fake/state/publisher/control/stale path from published verifier output. The exact production payload is independently built, receipted, architecture-audited, and executed through fail-before-network smoke. Together they verify source-generated serialization, trimming, child environment, provider/GitHub client separation, artifact bridge behavior, shared managed payload identity, and exact production artifact identity without claiming successful default-production GitHub wiring in PR/push CI.
3. One maintainer-authorized same-repository fixture PR and trusted default-branch workflow first prove the exact standalone production payload through its default composition against real GitHub, then prove two genuinely independent workflow runs. Run one bootstraps, publishes the sticky result, and accepts encrypted artifact state. Run two selects that state, decrypts and admits SESSION, proves verified-ahead continuation using prior-only evidence, publishes deterministically, advances lineage, and leaves public-safe readback. The pair also exercises one bounded stale-head or stale-writer case coherent with the fixture.

The trusted proof uses an explicitly prepared payload pinned to the merged implementation commit, the exact shared per-PR authoritative concurrency group, a deterministic provider unless a separately authorized live-provider run closes a named risk, and dedicated canaries. It proves both supported entrypoints cannot overlap for the same PR and that an unverified concurrency definition is rejected before candidate admission. Evidence records run IDs, workflow/action/payload SHAs, reviewed SHAs, public comment URLs/markers, non-linkable artifact counts, bounded pass/fail state outcomes, cleanup, and absence of keys/plaintext. Host-restricted evidence may retain artifact IDs, names, and digests needed for reconciliation, but the public record must not map any visible artifact identifier or producing run to the currently accepted lineage. It never publishes encrypted bytes merely as proof, never exposes secret values, and never claims that two jobs in one run satisfy independence.

Any proof PR, comment, review, artifact, workflow dispatch, secret/environment change, or repository metadata mutation requires explicit maintainer authorization at execution time. The implementation graph may prepare scripts and workflows without exercising credential-bearing mutation.

## R4-D14: activation and implementation graph record

The completed docs-gate PR added this contract and aligned architecture, security, distribution, roadmap, and README statements without adding implementation or mutating repository settings or secrets. It was Relay-reviewed on its exact pushed head and maintainer-merged before implementation issues were published.

After that merge, #138 published the following hierarchy. Tracker issues own no implementation PR. Each leaf has one primary PR, exact paths, acceptance criteria, local commands, and a Relay gate.

Every tracker has native type `Task`. The focused leaves use the reviewed native types below; publication must not replace them with one blanket type.

| Tracker                  | Focused leaf                                                                                                   | Native type | Primary ownership                                                                  | Blocks                                            |
| ------------------------ | -------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------- | ------------------------------------------------- |
| Host trust and snapshot  | Freeze Action inputs, trusted event/config materialization, and bounded Host result codes                      | Feature     | new C# ActionHost contracts/config plus contract tests                             | snapshot, state integration, wrapper              |
| Host trust and snapshot  | Implement GitHub identity authorization and exact-SHA tree/diff snapshot                                       | Feature     | C# GitHub client, snapshot materializer, tool-root integration                     | publisher transaction, integrated proof           |
| Encrypted artifact state | Migrate `IRestrictedStateStore` to async opaque operations and extract shared conformance                      | Task        | C# state contracts/service/local store/tests                                       | artifact adapter                                  |
| Encrypted artifact state | Implement the private Node artifact bridge and C# GitHub artifact adapter                                      | Feature     | wrapper bridge modules plus C# adapter/conformance                                 | production state integration, wrapper restoration |
| Encrypted artifact state | Integrate key resolution, bounded locator-root lifecycle, base-scope lineage head, rotation, reset, and state  | Feature     | C# ActionHost state composition/tests                                              | transaction coordinator, integrated proof         |
| Deterministic publisher  | Port R4 sticky renderer, marker, fingerprint, upsert, and readback                                             | Feature     | C# publisher modules/tests                                                         | transaction coordinator                           |
| Deterministic publisher  | Port opt-in inline mapping, batching, duplicate suppression, fallback, and partial warnings                    | Feature     | C# publisher modules/tests                                                         | final migration, integrated proof                 |
| Deterministic publisher  | Implement pre-provider recovery, ordering, intents, receipts, head barriers, ownership, and cancellation       | Feature     | C# ActionHost coordinator/tests                                                    | wrapper restoration, proof                        |
| Wrapper and migration    | Restore nested Action metadata and generated thin Node wrapper with build handshake and reproducibility checks | Feature     | `.github/actions/agentic-pr-review/**`, build scripts, wrapper tests, `dist:check` | integrated proof                                  |
| Wrapper and migration    | Remove replaced runtime/live-runtime/provider TypeScript business paths                                        | Task        | named `src/**` families and tests after replacement evidence                       | final residual audit                              |
| Wrapper and migration    | Remove replaced M4 state/acceptance/ledger/publisher/protocol families and close residual allowlists           | Task        | named `src/**`, schemas, fixtures, docs after mapped conformance                   | final residual audit                              |
| Wrapper and migration    | Run the final repository-wide migration, public-boundary, README, and distribution cutover audit               | Task        | package scripts, docs, CI, residual tests                                          | trusted proof                                     |
| Trusted proof and exit   | Add provider-secret-free framework and Linux x64 Native AOT integrated CI                                      | Task        | runtime integration fixtures/workflows                                             | trusted two-run proof                             |
| Trusted proof and exit   | Freeze and receipt the private trusted-proof payload and final E3 execution topology                           | Task        | proof payload, compiled workflow contract, proof control, offline preparation      | trusted workflow infrastructure                   |
| Trusted proof and exit   | Execute and record the maintainer-authorized two-independent-run product proof                                 | Task        | dedicated trusted workflow and fixture evidence                                    | R4 milestone close                                |

Dependencies are reciprocal: every leaf records what it blocks and what blocks it. The async store contract precedes the artifact adapter; event/config and adapter precede state integration; snapshot, state, and sticky publisher precede the transaction coordinator; the bridge and coordinator precede wrapper restoration; all replacements precede their deletion slice; the final residual audit and E2 precede R4-E2P; R4-E2P precedes E3 workflow infrastructure; and E3 precedes the separately authorized E4 two-run proof.

After the docs PR merges, #138 refreshes `main`, publishes and remotely verifies the approved native issue types, R4 milestone assignment, parent/sub-issue relationships, and dependency edges, posts the accepted decision/Relay lineage record, and closes only after the complete graph exists. R4 remains open until the implementation leaves and proof exit criteria complete.

## Security, privacy, and operational impact

The design treats public discovery of encrypted state artifacts and ciphertext download as an allowed worst-case visibility model; it does not claim that GitHub guarantees anonymous artifact download. Scoped artifact names are opaque, the generic sentinel name reveals only product use, plaintext is AEAD-protected and scope-bound, keys are downstream secrets, and untrusted routes cannot mutate trusted lineage or publish. This is a narrower and more honest guarantee than pretending public artifact existence can be hidden.

The highest residual risk is the lack of a transactional mutable artifact row. R4 handles it with authoritative GitHub workflow serialization for every supported same-PR side-effecting route, plus immutable candidate/acceptance records, unique-successor selection, exact readback, explicit conflict, and prior-lineage preservation. It does not call artifacts a platform-provided linearizable CAS. If the supported workflow definition cannot establish the shared one-running-run group, or bounded proof shows delayed listing or duplicate-successor behavior cannot meet the selected recovery model inside that group, implementation stops for a maintainer decision rather than weakening the contract silently.

The second residual risk is the privileged `workflow_run` boundary. R4 does not execute trigger artifacts, caches, scripts, checked-out reviewed code, or model-generated commands. All trigger and repository content remains data under strict bounds. Any future fork support, `pull_request_target`, external state service, OIDC federation, public adapter protocol, or automatic payload download requires a separate threat and compatibility decision.

## Validation and activation gates

This planning PR runs:

```bash
npm run check
npm run dist:check
```

At this point `dist:check` must still prove that the retired Action surface is absent. The wrapper-restoration leaf owns the later atomic semantic change.

R4 implementation is complete only when provider-secret-free PR/push CI, framework tests, Linux x64 Native AOT execution, state adapter conformance, exact snapshot tests, publisher/reconciliation tests, wrapper reproducibility, secret canaries, replacement/deletion audits, and the maintainer-authorized two-independent-run proof all pass. Public evidence may include opaque artifact metadata and ciphertext digests but never keys, plaintext SESSION bytes, provider continuation content, repository tool results, credentials, or raw provider bodies.
